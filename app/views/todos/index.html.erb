<h1>Todos</h1>

<%= link_to "New todo", new_todo_path if authenticated? %>


<section>

   <% @todos.each do |todo| %>

      <div>
         <a href="/todos/<%= todo.id %>">
            <%= todo.title %>
         </a>
      </div>

   <% end %>

</section>

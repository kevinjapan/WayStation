<section>

   <section class="artifact_bar">
      <div>
         <h4 class="light_grey_text">Home</h4>
         <h1>Programmes</h1>
      </div>
   </section>

   <section class="artifacts_list">

      <!-- to do : autoloading isn't working - researched, but nothing clear - suspect may be windows issue?
            perhaps we have to use ubunt
            tried w/ ubuntu - but autoload still not working.
      -->
      <ul>
         <% @programmes.each do |programme| %>
            <li><%= link_to programme.title, programme %></li>
         <% end %>
      </ul>
      <%= link_to "New programme", new_programme_path, class: "action_link" if authenticated? %>
   </section>
</section>
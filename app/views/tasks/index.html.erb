<h1>All Tasks</h1>

<!--  
to do : list project name alongside each task - from controller - see index in TasksController
-->

<section>
   <ul>
      <% @tasks.each do |task| %>
         <li><%= link_to task.title, task %><%= task.project_id %></li>
      <% end %>
   </ul>
</section>

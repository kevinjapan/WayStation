<h1>Tasks</h1>

<%= link_to "New task", new_task_path if authenticated? %>

<section>

   <% @tasks.each do |task| %>

      <div>
         <a href="/tasks/<%= task.id %>">
            <%= task.title %>
         </a>
      </div>

   <% end %>

</section>

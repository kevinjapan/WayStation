<h1>All Tasks</h1>

<section>
   <ul class="all_artifacts_list">
      <% @augmented_tasks.each do |task,project| %>
         <li>
            <%= link_to task.title, task %>
            <span class="parent_token">
               <span class="grey_text">project :  </span>
               <%= link_to project.title, project %>
            </span>
            
            <div>user: <%= task.users.distinct.pluck(:email_address) %></div>
            <div>creator: <%= task.creators.distinct.pluck(:email_address) %></div>


         </li>
      <% end %>
   </ul>
</section>

